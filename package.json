{
  "name": "inbox-to-md",
  "version": "0.1.1",
  "description": "Sync IMAP inboxes (OAuth or password) to Markdown files — one file per email, with YAML frontmatter",
  "type": "module",
  "bin": {
    "inbox-to-md": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "imap",
    "oauth",
    "gmail",
    "email",
    "markdown",
    "inbox",
    "sync",
    "cli"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chaidhat/inbox-to-md.git"
  },
  "homepage": "https://github.com/chaidhat/inbox-to-md#readme",
  "bugs": {
    "url": "https://github.com/chaidhat/inbox-to-md/issues"
  },
  "scripts": {
    "build": "tsc",
    "prebuild": "rm -rf dist",
    "prepublishOnly": "npm run build",
    "auth": "tsx src/commands/auth.ts",
    "dev": "tsx src/commands/sync.ts",
    "start": "tsx src/commands/sync.ts",
    "archive": "tsx src/commands/archive.ts",
    "compact": "tsx src/commands/compact.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/cli-progress": "^3.11.6",
    "@types/node": "^26.1.1",
    "@types/turndown": "^5.0.6",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.211",
    "cli-progress": "^3.12.0",
    "imapflow": "^1.4.7",
    "turndown": "^7.2.4"
  }
}
